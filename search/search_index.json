{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Myth/Betta","text":"<p><code>myth/betta</code> is a drop-in CodeIgniter 4 package that gives beta-stage apps a complete feedback collection and triage system. Install it, run the migration, and you've got a database-backed feedback store with a full CLI toolkit for reviewing, grouping, and prioritizing submissions \u2014 no admin UI required.</p>"},{"location":"#what-you-get","title":"What you get","text":"<ul> <li>A public-facing endpoint that accepts feedback via a standard form or <code>fetch()</code></li> <li>Two database tables with enum-typed columns and automatic CI4 model casting</li> <li><code>php spark feedback:*</code> commands for reviewing and clustering feedback from the terminal</li> <li>Optional AI-assisted clustering via <code>myth/scribe</code> \u2014 but it works great without it</li> </ul>"},{"location":"#quick-start","title":"Quick start","text":"<pre><code>composer require myth/betta\nphp spark migrate --all\n</code></pre> <p>That's it. Routes, commands, and migrations are all auto-discovered by CI4.</p>"},{"location":"#next-steps","title":"Next steps","text":"<ul> <li>Installation \u2014 requirements and optional config</li> <li>Database \u2014 what the two tables store</li> <li>Models \u2014 working with <code>FeedbackModel</code> and <code>FeedbackClusterModel</code></li> <li>Enums \u2014 the four enum types and how casting works</li> </ul>"},{"location":"browsing-feedback/","title":"Browsing Feedback","text":"<p>Once submissions start coming in, you'll want to triage them. The <code>feedback:list</code> Spark command gives you a quick filtered view straight from the terminal \u2014 no admin UI required.</p>"},{"location":"browsing-feedback/#basic-usage","title":"Basic usage","text":"<pre><code>php spark feedback:list\n</code></pre> <p>This returns up to 20 <code>new</code> items, sorted newest first:</p> <pre><code>+----+----------+--------+-----------+---------------------------------------------------+\n| ID | Category | Status | Cluster   | Message                                           |\n+----+----------+--------+-----------+---------------------------------------------------+\n| 42 | bug      | new    | \u2014         | The login button doesn't respond on mobile Safari |\n| 41 | ux       | new    | Login UX  | The password field loses focus when I tap it\u2026     |\n| 40 | feature  | new    | \u2014         | Could you add dark mode to the dashboard?         |\n+----+----------+--------+-----------+---------------------------------------------------+\n</code></pre> <p>Dismissed items are always hidden unless you ask for them explicitly.</p>"},{"location":"browsing-feedback/#flags","title":"Flags","text":""},{"location":"browsing-feedback/#-status","title":"<code>--status</code>","text":"<p>Filter by status. Accepted values match <code>StatusEnum</code>: <code>new</code>, <code>reviewed</code>, <code>grouped</code>, <code>dismissed</code>.</p> <pre><code>php spark feedback:list --status reviewed\nphp spark feedback:list --status dismissed\n</code></pre>"},{"location":"browsing-feedback/#-category","title":"<code>--category</code>","text":"<p>Filter by category. Accepted values: <code>bug</code>, <code>ux</code>, <code>feature</code>, <code>other</code>.</p> <pre><code>php spark feedback:list --category bug\n</code></pre> <p>Combine with <code>--status</code> to narrow further:</p> <pre><code>php spark feedback:list --category bug --status reviewed\n</code></pre>"},{"location":"browsing-feedback/#-cluster","title":"<code>--cluster</code>","text":"<p>Show only items belonging to a specific cluster. Pass the cluster ID.</p> <pre><code>php spark feedback:list --cluster 7\n</code></pre>"},{"location":"browsing-feedback/#-ungrouped","title":"<code>--ungrouped</code>","text":"<p>Show only items that haven't been assigned to a cluster yet.</p> <pre><code>php spark feedback:list --ungrouped\n</code></pre> <p>This combines additively with <code>--status</code>, so you can find unreviewed orphans:</p> <pre><code>php spark feedback:list --ungrouped --status new\n</code></pre>"},{"location":"browsing-feedback/#-limit","title":"<code>--limit</code>","text":"<p>Override the default 20-row cap.</p> <pre><code>php spark feedback:list --limit 50\n</code></pre>"},{"location":"browsing-feedback/#output-columns","title":"Output columns","text":"Column Description ID The feedback row's primary key Category <code>bug</code>, <code>ux</code>, <code>feature</code>, or <code>other</code> Status Current workflow status Cluster Cluster label, or <code>\u2014</code> if ungrouped Message First 50 characters of the submission (truncated with <code>\u2026</code> if longer)"},{"location":"browsing-feedback/#next-steps","title":"Next steps","text":"<ul> <li>Collecting Feedback \u2014 how submissions are saved in the first place</li> <li>Models \u2014 query the feedback table directly when you need more power</li> </ul>"},{"location":"changelog/","title":"Changelog","text":""},{"location":"changelog/#unreleased","title":"[Unreleased]","text":""},{"location":"collecting-feedback/","title":"Collecting Feedback","text":"<p>This page covers everything about the public-facing feedback endpoint: the routes that get auto-discovered, the config options that control behavior, the three views, and how to swap in your own templates.</p>"},{"location":"collecting-feedback/#how-it-works","title":"How it works","text":"<p>Installing the package gives you two routes automatically \u2014 no manual wiring needed:</p> Method URL Behavior <code>GET</code> <code>/feedback</code> Renders the feedback form (or the closed page) <code>POST</code> <code>/feedback/submit</code> Validates and saves a submission <p>Both routes respect your <code>$routePrefix</code> config, so if you change the prefix they both update together.</p> <p>The <code>POST</code> endpoint does content negotiation out of the box. Send <code>Accept: application/json</code> (or the <code>X-Requested-With: XMLHttpRequest</code> header) and you'll get a JSON response. Leave it as a plain form POST and you'll get a redirect with flash messages. The bundled form handles this automatically via <code>fetch()</code> with a graceful non-JS fallback.</p>"},{"location":"collecting-feedback/#configuration","title":"Configuration","text":"<p>Create <code>app/Config/Betta.php</code> in your host app to override any option:</p> <pre><code>&lt;?php\n\nnamespace Config;\n\nuse Myth\\Betta\\Config\\Betta as BettaConfig;\n\nclass Betta extends BettaConfig\n{\n    public string $routePrefix = 'feedback';\n    public bool $acceptSubmissions = true;\n}\n</code></pre>"},{"location":"collecting-feedback/#options","title":"Options","text":""},{"location":"collecting-feedback/#routeprefix","title":"<code>$routePrefix</code>","text":"<p>The URL segment for both routes. Defaults to <code>feedback</code>.</p> <pre><code>// Registers GET /beta-feedback and POST /beta-feedback/submit\npublic string $routePrefix = 'beta-feedback';\n</code></pre>"},{"location":"collecting-feedback/#acceptsubmissions","title":"<code>$acceptSubmissions</code>","text":"<p>Set to <code>false</code> to stop accepting new feedback without removing the routes. Visitors will see <code>closed.php</code> instead of the form.</p> <pre><code>public bool $acceptSubmissions = false;\n</code></pre> <p>Flip the switch without a deploy</p> <p>Since <code>Betta</code> extends <code>BaseConfig</code>, you can also drive <code>$acceptSubmissions</code> from a <code>.env</code> value: <code>betta.acceptSubmissions = false</code>. Useful for quickly closing the form without touching code.</p>"},{"location":"collecting-feedback/#what-gets-saved","title":"What gets saved","text":"<p>Every successful submission stores:</p> Field Source Notes <code>session_id</code> <code>sha256(session_id())</code> Pseudonymous \u2014 not reversible to a real session <code>category</code> POST <code>category</code> field One of <code>bug</code>, <code>ux</code>, <code>feature</code>, <code>other</code> <code>message</code> POST <code>message</code> field Required <code>email</code> POST <code>email</code> field Optional; validated if provided <code>url_context</code> POST <code>url_context</code> field, then <code>Referer</code> header The page the user was on when they submitted <p>The <code>url_context</code> hidden field is populated by JavaScript (<code>window.location.href</code>) before submission, so you get the exact page \u2014 not just the previous URL from the <code>Referer</code> header.</p>"},{"location":"collecting-feedback/#the-views","title":"The views","text":"<p>Three views ship with the package:</p> View Rendered when <code>page.php</code> <code>$acceptSubmissions</code> is <code>true</code> \u2014 the full standalone page <code>form.php</code> Embedded inside <code>page.php</code>; also embeddable in your own layouts <code>closed.php</code> <code>$acceptSubmissions</code> is <code>false</code>"},{"location":"collecting-feedback/#embedding-the-form-in-your-own-layout","title":"Embedding the form in your own layout","text":"<p>You don't have to use <code>page.php</code>. Render just the form fragment anywhere:</p> <pre><code>&lt;?= view('Myth\\Betta\\Views\\form', [\n    'categories' =&gt; \\Myth\\Betta\\Enums\\CategoryEnum::cases(),\n    'submitUrl'  =&gt; config(\\Myth\\Betta\\Config\\Betta::class)-&gt;routePrefix . '/submit',\n]) ?&gt;\n</code></pre>"},{"location":"collecting-feedback/#overriding-views","title":"Overriding views","text":"<p>Drop a replacement file into your host app at <code>app/Views/vendor/betta/{view}.php</code> and it takes precedence over the package view. The controller checks that path first.</p> <pre><code>app/\n  Views/\n    vendor/\n      betta/\n        form.php      \u2190 overrides the package form\n        page.php      \u2190 overrides the full-page wrapper\n        closed.php    \u2190 overrides the closed state page\n</code></pre> <p>You only need to create the files you want to override \u2014 the rest fall back to the package defaults.</p>"},{"location":"collecting-feedback/#response-formats","title":"Response formats","text":""},{"location":"collecting-feedback/#json-fetch-path","title":"JSON (fetch path)","text":"<p>Send <code>Accept: application/json</code> to get structured responses:</p> <p>Success (200) <pre><code>{ \"ok\": true }\n</code></pre></p> <p>Validation failure (422) <pre><code>{\n  \"errors\": {\n    \"message\": \"The message field is required.\"\n  }\n}\n</code></pre></p>"},{"location":"collecting-feedback/#plain-post","title":"Plain POST","text":"<ul> <li>Success \u2192 redirect to <code>/{routePrefix}</code> with a <code>feedback_success</code> flash message</li> <li>Validation failure \u2192 redirect back with an <code>errors</code> flash and repopulated input via <code>withInput()</code></li> </ul> <p>The bundled <code>form.php</code> handles both paths \u2014 it reads flash messages on load and intercepts submissions with <code>fetch()</code> when JavaScript is available.</p>"},{"location":"collecting-feedback/#next-steps","title":"Next steps","text":"<ul> <li>Database \u2014 the schema behind feedback storage</li> <li>Models \u2014 querying and working with saved feedback</li> <li>Enums \u2014 the category, status, sentiment, and priority types</li> </ul>"},{"location":"database/","title":"Database","text":"<p>Running <code>php spark migrate --all</code> creates two tables. Here's what each one stores.</p>"},{"location":"database/#betta_feedback","title":"<code>betta_feedback</code>","text":"<p>Each row is a single piece of feedback submitted by a user.</p> Column Type Notes <code>id</code> INT UNSIGNED PK Auto-increment <code>session_id</code> VARCHAR(64) SHA-256 of <code>session_id()</code> \u2014 anonymous identity across submissions <code>email</code> VARCHAR(255) Optional contact address <code>category</code> VARCHAR(20) One of <code>bug</code>, <code>ux</code>, <code>feature</code>, <code>other</code> \u2014 default <code>other</code> <code>message</code> TEXT The feedback body \u2014 required <code>url_context</code> VARCHAR(500) The page the user was on (<code>window.location.href</code>, fallback to <code>HTTP_REFERER</code>) <code>sentiment</code> TINYINT <code>-1</code> negative, <code>0</code> neutral, <code>1</code> positive \u2014 nullable <code>status</code> VARCHAR(20) One of <code>new</code>, <code>reviewed</code>, <code>grouped</code>, <code>dismissed</code> \u2014 default <code>new</code> <code>cluster_id</code> INT UNSIGNED FK to <code>feedback_clusters.id</code> \u2014 nullable <code>created_at</code> DATETIME <code>updated_at</code> DATETIME <p>The enum-like columns (<code>category</code>, <code>status</code>, <code>sentiment</code>) are <code>VARCHAR</code>/<code>TINYINT</code> \u2014 no DB-level ENUM type \u2014 so the schema works across MySQL, SQLite, PostgreSQL, and SQLSRV without modification.</p>"},{"location":"database/#feedback_clusters","title":"<code>feedback_clusters</code>","text":"<p>Clusters are groupings you create to organize related feedback. They have no <code>item_count</code> column \u2014 the count is always computed live via a <code>COUNT()</code> join, so it can never drift out of sync.</p> Column Type Notes <code>id</code> INT UNSIGNED PK Auto-increment <code>label</code> VARCHAR(255) Short name for the cluster \u2014 required <code>summary</code> TEXT Manual or AI-generated description \u2014 nullable <code>priority</code> VARCHAR(20) One of <code>low</code>, <code>medium</code>, <code>high</code>, <code>critical</code> \u2014 default <code>medium</code> <code>created_at</code> DATETIME <code>updated_at</code> DATETIME"},{"location":"database/#next-steps","title":"Next steps","text":"<ul> <li>Models \u2014 reading and writing feedback via CI4 models</li> <li>Enums \u2014 the PHP enum types that back these columns</li> </ul>"},{"location":"enums/","title":"Enums","text":"<p>The four enums in <code>Myth\\Betta\\Enums\\</code> back the enum-typed columns in <code>betta_feedback</code> and <code>feedback_clusters</code>. CI4's model layer casts these automatically on read, so you always get enum instances back from a <code>find()</code> call.</p>"},{"location":"enums/#categoryenum","title":"CategoryEnum","text":"<p>Classifies what kind of feedback was submitted.</p> <pre><code>use Myth\\Betta\\Enums\\CategoryEnum;\n\nCategoryEnum::Bug      // 'bug'\nCategoryEnum::UX       // 'ux'\nCategoryEnum::Feature  // 'feature'\nCategoryEnum::Other    // 'other'  \u2190 column default\n</code></pre> <p>Column: <code>betta_feedback.category</code> (VARCHAR 20, default <code>other</code>)</p>"},{"location":"enums/#statusenum","title":"StatusEnum","text":"<p>Tracks where a feedback item is in the triage workflow.</p> <pre><code>use Myth\\Betta\\Enums\\StatusEnum;\n\nStatusEnum::New        // 'new'       \u2190 column default, needs review\nStatusEnum::Reviewed   // 'reviewed'  set automatically on view\nStatusEnum::Grouped    // 'grouped'   assigned to a cluster\nStatusEnum::Dismissed  // 'dismissed' hidden from default list\n</code></pre> <p>Column: <code>betta_feedback.status</code> (VARCHAR 20, default <code>new</code>)</p>"},{"location":"enums/#priorityenum","title":"PriorityEnum","text":"<p>Ranks how urgently a cluster needs attention.</p> <pre><code>use Myth\\Betta\\Enums\\PriorityEnum;\n\nPriorityEnum::Low       // 'low'\nPriorityEnum::Medium    // 'medium'  \u2190 column default\nPriorityEnum::High      // 'high'\nPriorityEnum::Critical  // 'critical'\n</code></pre> <p>Column: <code>feedback_clusters.priority</code> (VARCHAR 20, default <code>medium</code>)</p>"},{"location":"enums/#sentimentenum","title":"SentimentEnum","text":"<p>Records the emotional tone of a feedback submission. It's int-backed so it sorts naturally.</p> <pre><code>use Myth\\Betta\\Enums\\SentimentEnum;\n\nSentimentEnum::Negative  // -1\nSentimentEnum::Neutral   //  0\nSentimentEnum::Positive  //  1\n</code></pre> <p>Column: <code>betta_feedback.sentiment</code> (TINYINT, nullable \u2014 omit if unknown)</p>"},{"location":"enums/#using-enums-in-queries","title":"Using enums in queries","text":"<p>All four enums are native PHP backed enums, so you can use <code>cases()</code>, <code>from()</code>, and <code>tryFrom()</code> as usual:</p> <pre><code>// Get all feedback that still needs review\n$items = (new FeedbackModel())\n    -&gt;where('status', StatusEnum::New-&gt;value)\n    -&gt;findAll();\n\n// Convert a user-supplied string safely\n$category = CategoryEnum::tryFrom($input) ?? CategoryEnum::Other;\n</code></pre> <p>Pass instances, not values, to model methods</p> <p>When inserting or updating via the model, pass the enum instance (<code>StatusEnum::Reviewed</code>), not the backing value (<code>'reviewed'</code>). The model's write cast expects an enum object. Passing a string will throw a <code>CastException</code>.</p>"},{"location":"enums/#next-steps","title":"Next steps","text":"<ul> <li>Models \u2014 how the models use these enums for casting</li> <li>Database \u2014 the column definitions behind each enum</li> </ul>"},{"location":"installation/","title":"Installation","text":""},{"location":"installation/#requirements","title":"Requirements","text":"<ul> <li>PHP 8.2+</li> <li>CodeIgniter 4.5+ (required for PHP enum model casting)</li> </ul>"},{"location":"installation/#install-the-package","title":"Install the package","text":"<pre><code>composer require myth/betta\nphp spark migrate --all\n</code></pre> <p>CI4 auto-discovers the package \u2014 routes, Spark commands, and migrations are all wired up with no manual bootstrapping.</p>"},{"location":"installation/#optional-ai-clustering","title":"Optional: AI clustering","text":"<p>To use <code>spark feedback:analyze</code>, install <code>myth/scribe</code> and configure an API key:</p> <pre><code>composer require myth/scribe\nphp spark config:publish Myth\\Scribe\\Config\\AI\n</code></pre> <p>Then add your key to <code>.env</code>:</p> <pre><code>CLAUDE_API_KEY=sk-ant-...\n# or OPENAI_API_KEY=sk-...\n</code></pre> <p><code>myth/betta</code> works fine without Scribe \u2014 the <code>feedback:analyze</code> command just exits cleanly if it isn't installed.</p>"},{"location":"installation/#optional-override-config","title":"Optional: override config","text":"<p>You don't need to publish the config to override it. Create <code>app/Config/Betta.php</code> and extend the package class:</p> <pre><code>&lt;?php\n\nuse Myth\\Betta\\Config\\Betta as BaseBetta;\n\nclass Betta extends BaseBetta\n{\n    public string $routePrefix = 'betta';   // default: 'feedback'\n    public bool $acceptSubmissions = false;  // close the form\n    public int $maxMessageLength = 1000;     // default: 2000\n}\n</code></pre> <p>CI4's config cascade picks this up automatically \u2014 no publish step needed.</p>"},{"location":"installation/#next-steps","title":"Next steps","text":"<ul> <li>Database \u2014 what the two tables store</li> <li>Models \u2014 working with <code>FeedbackModel</code> and <code>FeedbackClusterModel</code></li> </ul>"},{"location":"managing-clusters/","title":"Managing Clusters","text":"<p>Clusters let you group related feedback items together \u2014 \"Login UX issues\", \"Mobile crashes\", \"Onboarding requests\" \u2014 so you can triage in bulk instead of item by item. These four Spark commands cover the full lifecycle.</p>"},{"location":"managing-clusters/#listing-clusters","title":"Listing clusters","text":"<pre><code>php spark feedback:clusters\n</code></pre> <p>This shows all clusters sorted by most recently updated:</p> <pre><code>+----+------------------+----------+-------+---------------------+\n| ID | Label            | Priority | Items | Updated             |\n+----+------------------+----------+-------+---------------------+\n|  3 | Login UX Issues  | high     |    12 | 2026-06-01 14:22:00 |\n|  1 | Onboarding Asks  | medium   |     5 | 2026-05-30 09:10:00 |\n|  2 | Mobile Crashes   | critical |     3 | 2026-05-28 17:45:00 |\n+----+------------------+----------+-------+---------------------+\n</code></pre> <p>Item count is always computed live \u2014 it reflects how many feedback rows are currently assigned to the cluster.</p>"},{"location":"managing-clusters/#-priority","title":"<code>--priority</code>","text":"<p>Filter to clusters of a specific priority. Accepted values: <code>low</code>, <code>medium</code>, <code>high</code>, <code>critical</code>.</p> <pre><code>php spark feedback:clusters --priority high\n</code></pre>"},{"location":"managing-clusters/#-sort","title":"<code>--sort</code>","text":"<p>Change the sort order. Default is <code>updated_at</code> (most recently active first). Use <code>count</code> to float the most-populated clusters to the top.</p> <pre><code>php spark feedback:clusters --sort count\n</code></pre>"},{"location":"managing-clusters/#creating-a-cluster","title":"Creating a cluster","text":"<pre><code>php spark feedback:cluster:create \"Login UX Issues\"\n</code></pre> <p>Prints the new cluster's ID on success:</p> <pre><code>7\n</code></pre> <p>Use <code>--priority</code> to set the priority at creation time. If you skip it, priority defaults to <code>medium</code>.</p> <pre><code>php spark feedback:cluster:create \"Critical Auth Bugs\" --priority critical\n</code></pre>"},{"location":"managing-clusters/#editing-a-cluster","title":"Editing a cluster","text":"<pre><code>php spark feedback:cluster:edit &lt;id&gt; [--label=\"...\"] [--priority=&lt;val&gt;]\n</code></pre> <p>Update the label, the priority, or both. You need to pass at least one flag \u2014 the command errors if you don't.</p> <pre><code># Rename only\nphp spark feedback:cluster:edit 7 --label \"Login &amp; Auth Issues\"\n\n# Change priority only\nphp spark feedback:cluster:edit 7 --priority critical\n\n# Update both at once\nphp spark feedback:cluster:edit 7 --label \"Login &amp; Auth Issues\" --priority critical\n</code></pre> <p>If the ID doesn't exist you'll get an error rather than a silent no-op.</p>"},{"location":"managing-clusters/#deleting-a-cluster","title":"Deleting a cluster","text":"<pre><code>php spark feedback:cluster:delete &lt;id&gt;\n</code></pre> <p>Before anything is changed, the command asks you to confirm:</p> <pre><code>Delete cluster #7 \"Login &amp; Auth Issues\" and ungroup all its items? [yes, no]:\n</code></pre> <p>Type <code>yes</code> to proceed or anything else to abort.</p> <p>What happens to items when you delete a cluster:</p> <ul> <li>Items with status <code>new</code>, <code>reviewed</code>, or <code>grouped</code> have their <code>cluster_id</code> cleared and status reset to <code>new</code>. They go back into the unreviewed pile.</li> <li>Items with status <code>dismissed</code> are left exactly as they are \u2014 their <code>cluster_id</code> is preserved and their status doesn't change.</li> </ul> <p>Deletion is not reversible</p> <p>There's no undo. If you want to preserve the grouping, consider renaming or reprioritising the cluster instead of deleting it.</p>"},{"location":"managing-clusters/#assigning-an-item-to-a-cluster","title":"Assigning an item to a cluster","text":"<pre><code>php spark feedback:group &lt;id&gt; &lt;cluster_id&gt;\n</code></pre> <p>Sets <code>cluster_id</code> and flips the item's status to <code>grouped</code> in one step. Both arguments are required.</p> <pre><code>php spark feedback:group 142 3\n</code></pre> <p>On success:</p> <pre><code>Feedback 142 assigned to cluster 3.\n</code></pre> <p>If either ID doesn't exist you'll get a clear error and a non-zero exit code:</p> <pre><code>Feedback item 142 not found.\n</code></pre> <pre><code>Cluster 3 not found.\n</code></pre> <p>The non-zero exit makes it safe to use in scripts \u2014 failures won't silently pass through <code>&amp;&amp;</code> chains.</p>"},{"location":"managing-clusters/#bulk-reassignment","title":"Bulk reassignment","text":"<p>Because the command is non-interactive, it's easy to loop over a list of IDs in a shell script:</p> <pre><code>for id in 142 143 144 145; do\n    php spark feedback:group \"$id\" 3\ndone\n</code></pre> <p>Or pipe IDs from another command:</p> <pre><code>php spark feedback:list --status new --limit 50 | awk '{print $1}' | xargs -I{} php spark feedback:group {} 3\n</code></pre> <p>Finding item IDs</p> <p>Use <code>php spark feedback:list</code> to browse unassigned items and note their IDs before running a bulk assignment.</p>"},{"location":"managing-clusters/#next-steps","title":"Next steps","text":"<ul> <li>Browsing Feedback \u2014 filter and view individual items, including by cluster</li> <li>Models \u2014 query clusters and feedback directly when you need more than the CLI offers</li> </ul>"},{"location":"models/","title":"Models","text":"<p>The package ships two CI4 models. Both extend <code>CodeIgniter\\Model</code> and use CI4 4.5's native enum casting, so reading a row always gives you proper PHP enum instances \u2014 no string comparisons needed.</p>"},{"location":"models/#feedbackmodel","title":"FeedbackModel","text":"<p>Manages the <code>betta_feedback</code> table.</p> <pre><code>use Myth\\Betta\\Models\\FeedbackModel;\n\n$model = new FeedbackModel();\n</code></pre>"},{"location":"models/#inserting-feedback","title":"Inserting feedback","text":"<p>Pass enum instances directly \u2014 the model's cast layer handles serialisation to the DB value.</p> <pre><code>use Myth\\Betta\\Enums\\CategoryEnum;\nuse Myth\\Betta\\Enums\\SentimentEnum;\nuse Myth\\Betta\\Enums\\StatusEnum;\n\n$id = $model-&gt;insert([\n    'message'   =&gt; 'The onboarding flow is confusing on mobile.',\n    'category'  =&gt; CategoryEnum::UX,\n    'sentiment' =&gt; SentimentEnum::Negative,\n    'email'     =&gt; 'user@example.com',\n]);\n</code></pre> <p>Omitting <code>category</code>, <code>status</code>, or <code>sentiment</code> uses the column defaults (<code>other</code>, <code>new</code>, <code>null</code>).</p>"},{"location":"models/#reading-feedback","title":"Reading feedback","text":"<p><code>find()</code> and <code>findAll()</code> return <code>stdClass</code> objects with enum instances in the cast fields:</p> <pre><code>$row = $model-&gt;find($id);\n\n$row-&gt;category;  // CategoryEnum::UX\n$row-&gt;status;    // StatusEnum::New\n$row-&gt;sentiment; // SentimentEnum::Negative  (or null if not set)\n</code></pre> <p>You can compare directly with enum cases:</p> <pre><code>if ($row-&gt;status === StatusEnum::New) {\n    // needs review\n}\n</code></pre>"},{"location":"models/#updating-a-row","title":"Updating a row","text":"<pre><code>$model-&gt;update($id, ['status' =&gt; StatusEnum::Reviewed]);\n</code></pre> <p>Always pass enum instances, not strings</p> <p>The model's <code>set()</code> cast expects enum instances on write. Passing the raw string <code>'reviewed'</code> instead of <code>StatusEnum::Reviewed</code> will throw a <code>CastException</code>.</p>"},{"location":"models/#validation","title":"Validation","text":"<p>The model validates automatically on insert and update. The rules guard enum values at the application layer:</p> Field Rule <code>message</code> required <code>category</code> must be one of <code>bug</code>, <code>ux</code>, <code>feature</code>, <code>other</code> <code>status</code> must be one of <code>new</code>, <code>reviewed</code>, <code>grouped</code>, <code>dismissed</code> <code>sentiment</code> must be <code>-1</code>, <code>0</code>, or <code>1</code> if provided"},{"location":"models/#feedbackclustermodel","title":"FeedbackClusterModel","text":"<p>Manages the <code>feedback_clusters</code> table.</p> <pre><code>use Myth\\Betta\\Models\\FeedbackClusterModel;\n\n$model = new FeedbackClusterModel();\n</code></pre>"},{"location":"models/#creating-a-cluster","title":"Creating a cluster","text":"<pre><code>use Myth\\Betta\\Enums\\PriorityEnum;\n\n$id = $model-&gt;insert([\n    'label'    =&gt; 'Onboarding UX issues',\n    'priority' =&gt; PriorityEnum::High,\n    'summary'  =&gt; 'Multiple users struggling with the first-run flow on mobile.',\n]);\n</code></pre>"},{"location":"models/#reading-a-cluster","title":"Reading a cluster","text":"<pre><code>$cluster = $model-&gt;find($id);\n\n$cluster-&gt;label;    // 'Onboarding UX issues'\n$cluster-&gt;priority; // PriorityEnum::High\n</code></pre>"},{"location":"models/#findallwithcount","title":"findAllWithCount()","text":"<p>The standard <code>findAll()</code> returns clusters without item counts. Use <code>findAllWithCount()</code> when you need the number of feedback items in each cluster \u2014 it computes the count via a <code>COUNT()</code> join so it's always accurate.</p> <pre><code>$clusters = $model-&gt;findAllWithCount();\n\nforeach ($clusters as $cluster) {\n    echo $cluster-&gt;label;      // string\n    echo $cluster-&gt;item_count; // int \u2014 always current\n    echo $cluster-&gt;priority-&gt;value; // 'high' \u2014 still a PriorityEnum instance\n}\n</code></pre> <p>item_count is not stored</p> <p>There's no <code>item_count</code> column. The count comes from a live <code>LEFT JOIN</code> against <code>betta_feedback</code>. This means it can never drift \u2014 reassigning or deleting feedback updates the count automatically on the next query.</p>"},{"location":"models/#next-steps","title":"Next steps","text":"<ul> <li>Enums \u2014 all four enum types and their cases</li> <li>Database \u2014 the underlying table structure</li> </ul>"}]}